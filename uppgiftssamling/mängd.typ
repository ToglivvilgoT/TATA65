+ #pad(bottom: 1.5em)[
  Är det sant att $A union (accent(A, -) inter accent(B, -) inter accent(C, -)) = A union B union accent(C, -)$ för alla mängder A, B, C?

  Lösning: kan till exempel visas med hjälp av Venndiagram.

  Svar: Likheten är inte sann. 
]

+ #pad(bottom: 1.5em)[
  Givet $A = {emptyset, {emptyset}}$ stämmer följande påståenden?

  a) $emptyset in A$?

  b) ${emptyset} in A$?

  c) ${{emptyset}} in A$?

  d) $emptyset subset.eq A$?

  e) ${emptyset} subset.eq A$?

  f) ${{emptyset}} subset.eq A$?
]

+ #pad(bottom: 1.5em)[
  Låt $A, B$ vara 2 mängder.
  Vilka av följande påståenden stämmer?
  Om ett påstående stämmer, bevisa det.
  Om ett påstående inte stämmer, ge ett exempel på $A, B$ som motbevisar påståendet.

  a) Om $B subset.eq A$ så måste $accent(A, -) subset.eq accent(B, -)$

  b) Om $A \\ B = emptyset$ så måste $A = B$

  c) $accent(A triangle B, -) = A inter B$

  d) $accent((accent(A, -) \\ B) union accent((A union B), -), -) inter accent(emptyset, -)= A union B$
]

+ #pad(bottom: 1.5em)[
  Givet $A = {x^2 + 3x - 5 : x " är ett udda positivt heltal och" x <= 6} "och" B = {sqrt(-1), pi, e, sqrt(5), pi}$. Beräkna:

  a) $abs(A) + abs(B)$

  b) $abs(A union B)$

  c) $abs(P(B)) - abs(P(A))$

  d) $abs(P(B) \\ P(A))$

  e) $abs(P(P(A) union P(B)))$

  f) $abs(P(P(P(A) inter P(B))))$
]

+ #pad(bottom: 1.5em)[
  Låt $A = {1, 2, 3}, B = {a, b}$. Beräkna:

  a) $A times B$

  b) $abs(A times B)$

  c) $abs(A) dot abs(B)$

  d) $abs((A union B) times (A inter B))$
]

+ #pad(bottom: 1.5em)[
  Är $(A times B) times C = A times (B times C) "för alla mängder" A, B, C?$ Om sant, bevisa det och om inte ge exempel på $A, B, C$ där likheten inte stämmer.
]